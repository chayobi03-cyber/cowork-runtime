# Decision Log — cowork-runtime

Coworkai detailed_rules 7절 "구조화 산출물 선택 기준"의 decision log 형식(결정/근거/변경 사유/
대체안)을 따른다.

---

## D-001: 범용 콘텐츠는 어디에 둘 것인가 — Git 단독도, 프로젝트 지침 단독도 아닌 역할 분리

**날짜**: 2026-07-12

**결정**: 콘텐츠 성격에 따라 둘로 나눈다.
- 자주 안 바뀌는 원본(base_instructions, detailed rules 본문) → **Git** (`cowork-runtime` 저장소)
- 프로젝트 식별 정보 + "이 저장소를 봐라"는 포인터 → **각 AI 프로젝트의 커스텀 지침 칸**

**검토했던 대안**

1. **Git 단독**: 모든 것을 저장소 하나로. 단일 소스·타 AI 접근성은 강하지만, 이 컨테이너는
   태스크 종료 시 파일시스템이 초기화되므로(computer_use 환경 규칙) 다음 세션에서 **쓰기(push)는
   매번 device-flow 인증을 처음부터 다시 거쳐야 함**. 읽기(clone)는 Public repo라 인증 없이 되지만,
   "자동으로 매 세션 반영"은 Claude가 능동적으로 clone할 때만 작동 — 완전 자동은 아님.
2. **프로젝트 지침 단독**: 각 Claude Project 커스텀 지침 칸에 전체 내용을 복사. 편집은 쉽지만,
   프로젝트마다 사본이 생겨 드리프트 발생 — 실제로 이번 세션에서 겪은 문제(mvp_school 파일명 v0.5.4
   vs 내부 v0.5.3, GPT 백업이 traceable_emc v0.4.2 미반영 등)와 동일한 실패 패턴. 또한 GPT 등
   Claude Project 설정에 접근 못 하는 다른 AI와는 애초에 공유가 안 됨 — "다른 프로젝트/AI에도
   범용으로" 라는 목표와 정면으로 배치.

**근거 (audit 기준)**

- 파일·상태·산출물 traceability: 원본 하나(git)로 모아야 diff/이력 추적 가능
- 운영자 실수 가능성: 사본이 여러 개면 어느 게 최신인지 매번 판단해야 함(8절의 "최신 첨부본이
  source of truth" 규칙이 애초에 이 문제의 사후 땜질이었음) — 사본을 안 만드는 게 근본 해결
- 실제 엔지니어 업무 감소 효과: 자주 안 바뀌는 원본을 git에 두면 쓰기 비용(인증)이 드물게만 발생.
  자주 바뀌는 포인터/식별정보는 지침 칸에 둬서 창엽님이 타이핑 한 번으로 바로 갱신 가능

**변경 시 재검토 트리거**

- 이 저장소를 여러 세션에서 반복적으로 push해야 하는 작업이 잦아져서, 매번 device-flow 인증이
  실제로 병목이 된다는 사례가 2회 이상 쌓이면(2.2 임계치 재사용) — 그때 CI/PAT 상시 발급 등
  대안 재검토

**대체안 채택 여부**: 채택 안 함(둘 다 단독으로는 부적합, 위 역할분리로 대체)

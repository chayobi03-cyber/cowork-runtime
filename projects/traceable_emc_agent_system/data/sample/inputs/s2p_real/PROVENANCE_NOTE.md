# data/sample/inputs/s2p_real/ — 데이터 출처 안내 (Claude 관리 추가)

이 폴더의 3개 파일은 실제 존재하는 부품번호를 파일명으로 쓰고 있습니다.

```text
BLM15PX121SN1_series.s2p.txt
BLM18SG221TN1_series.s2p.txt
LQW15CAR11J00_series.s2p.txt
```

BLM/LQW 접두사는 Murata의 실제 ferrite bead/inductor 부품 시리즈 명명 규칙과 일치합니다.

## 확인되지 않은 것

이 데이터가 다음 중 무엇인지 저장소 내 어떤 문서에도 명시되어 있지 않습니다.

1. 제조사 공개 데이터시트에서 추출한 실측치
2. 실측치를 참고해 만든 합성(synthetic) 근사치
3. 순수 합성 데이터에 실제 부품명만 붙인 것

## 왜 이게 문제가 되는가 (Coworkai audit 항목 8: 라이선스·출처 리스크)

- 실측 데이터시트 추출본이라면 제조사 라이선스/재배포 조건 확인이 필요합니다.
- 합성 데이터인데 실제 부품명이 붙어 있으면, 나중에 이 값을 실제 부품 특성으로
  오인해 설계 판단에 쓰는 사고로 이어질 수 있습니다.

## Claude가 하지 않은 것

이 데이터의 실제 출처를 추정하거나 확정하지 않았습니다. 값의 패턴만으로 실측/합성
여부를 단정하는 것은 근거 없는 주장이 되므로, 사실 확인이 필요하다는 것만 기록합니다.

## 다음 액션 (사람 확인 필요)

1. 이 3개 파일을 원래 어디서 가져왔는지 확인(작성자 기억, 이전 대화 이력, 원본 다운로드 링크 등)
2. 실측이면 출처 URL/데이터시트 버전을 이 폴더에 `SOURCE.md`로 남기기
3. 합성이면 "synthetic, for pipeline testing only, not real part characteristics"라고
   각 파일 상단에 주석으로 남기기

# AI_CONTEXT.md

이 문서는 AI(Claude/GPT 등)가 이 파트 백서 작업을 도울 때 가장 먼저 읽는 1페이지 요약이다.

## 이 파트는 무엇을 하는 곳인가

약 20명 규모, 3개 소파트로 구성된 엔지니어링 파트다. 반복 업무·검토 업무·시험/해석 데이터 정리
업무가 존재하며, 이 업무들을 자동화 후보(Topic)로 등록하고 조사·실증해서 이어받을 수 있는
형태로 남기는 것이 목표다.

## v1이 실제로 만드는 것 (AI가 헷갈리지 말아야 할 지점)

**v1은 AI agent를 만들지 않는다.** v1이 만드는 건 업무를 원자 단위(Topic Card 하나 = 업무
하나)로 쪼개서 AI가 접근·검색·수정·재사용하기 쉬운 구조다. "업무 효율화 agent 생성"은
`ROADMAP.md`에 있는 장기 목표이며, 원자화된 구조가 갖춰진 뒤의 다음 단계다. 지금 백서 관련
요청이 오면 "이 구조를 원자화/AI접근성 기준으로 정리하는 작업"인지 먼저 판단하고, agent 구현
요청처럼 보이면 ROADMAP.md의 마일스톤 단계와 맞는지 먼저 확인한다.

## 지금 이 백서는 무슨 상태인가 (v1, 착수 단계)

- 완성된 운영 체계가 아니라 **부서 회의 전 최소 초안**이다.
- 실제 사내 시스템(공유폴더/Git/문서시스템 가용 범위)은 아직 미확인 상태다.
- 운영 위치, Role/Workstream 정식 체계, Stable Spine ADR 절차는 전부 회의 이후로 보류돼 있다.
- 지금 존재하는 건 Topic Registry 1개 + Topic Card 템플릿 1개 + 요약 본문 2개뿐이다.

## AI가 절대 하면 안 되는 추론 (금지 목록)

- `candidate_mvp` = 구현 완료로 해석하지 않는다.
- `company_only_validation` = 이미 검증됨으로 해석하지 않는다.
- 백서에 언급됐다 = 승인됐다로 해석하지 않는다.
- Topic Registry 등록 = 진행 중 프로젝트로 해석하지 않는다.
- Fixture 통과 = 프로덕션 준비 완료로 해석하지 않는다.
- Role owner 필드 = 실제 인력 배정 완료로 해석하지 않는다.

## AI가 지켜야 할 것

- 회사 내부 정보(제품명/고객명/경로/개인이름/credential/raw 시험결과)는 외부 AI 입력용 export에
  절대 포함하지 않는다. export 전 `EXPORT_SAFETY_CHECKLIST.md`를 반드시 통과시킨다.
- 새 Topic은 백서 본문에 직접 쓰지 않고 Topic Card로 받아 Registry에 등록한다.
- 이 파트의 자동화 관련 실행 코드/검증은 Coworkai 프로젝트(별도 저장소, cowork-runtime)의
  Track/Audit 규칙을 그대로 준용한다. 이 백서 프로젝트 자체는 아직 Research Track이다
  (실행 코드 없음, 구조·원칙 설계 단계).
- 화면캡처/화면녹화 분석은 **PC 환경을 메인으로 처리**한다. 휴대폰에서 온 사진·영상은 바로
  분석하지 않고 PC로 옮겨진 뒤 처리한다고 가정한다(휴대폰은 텍스트·음성메모만 우선 입구로
  취급). 다중 모니터·특정 창 단위로 캡처 범위가 이미 좁혀졌는지 확인하고, 안 좁혀졌으면
  그 사실을 먼저 알린다 (`WRITING_METHODOLOGY.md` C-06 참조).
- **오디오/영상 파일을 직접 재생·분석할 수 없다** (2026-07-12 검증). 음성은 사람이 먼저
  텍스트로 전사한 것만 처리 가능하고, 화면녹화는 사람이 핵심 장면을 스크린샷으로 미리 추출한
  것만 이미지로 처리 가능하다. 녹화/음성 파일이 그대로 오면 이 제약을 먼저 안내한다.
- **카드 입력을 도와달라는 요청이 오면 `WRITING_METHODOLOGY.md` D절(D-01~D-05)을 그대로
  따른다** — 원본 확인 → Export Safety 선확인 → Level A 필드 채움 → 불확실 지점 표시 → 결과
  제시 후 대기. 순서를 바꾸거나 생략하지 않는다.

## 참고 문서

- 상세 판단 근거: `SECTION_BY_SECTION_REVIEW.md`, `EXTERNAL_STRUCTURE_BENCHMARK_REPORT.md`
- 운영 원칙 요약: `01_PART_WORK_WHITEPAPER.md`
- **백서 작성 방법론**: `WRITING_METHODOLOGY.md` — 문서 갱신 원칙, 파트원 채택 저항 최소화,
  입력 방식(텍스트/음성/스크린샷/화면녹화) 설계 근거. 새 방법론 아이디어는 이 문서의 해당 절
  표에 항목만 추가하는 방식으로 갱신한다 (문서 구조 자체는 안 바뀜).
- **로드맵**: `ROADMAP.md` — v1 이후 마일스톤(M1 지식그래프 연결 / M2 입력 자동화 / M3 agent
  생성)과 착수 조건. v1 범위 밖 아이디어는 여기에 마일스톤으로 배치하고 즉시 착수하지 않는다.
